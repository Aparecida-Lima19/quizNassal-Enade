import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxTitle: {
        alignItems: "center",
        justifyContent: "center",
        padding: 10,
    },
    textTitle: {
        color: "#FFFFFF",
        //o texto não ficou em branco...
        fontSize: 25,
        fontWeight: "bold"
    },
    
});

export default styles;